[project]
name = "snp-priortization"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Christian Ramirez", email = "christian.ramirez1@umassmed.edu" }
]
requires-python = ">=3.12"
dependencies = [
    "loguru>=0.7.3",
    "polars[all]>=1.41.0",
    "pysam>=0.24.0",
]

[build-system]
requires = ["uv_build>=0.11.16,<0.12.0"]
build-backend = "uv_build"

[dependency-groups]
dev = [
    "marimo>=0.23.8",
    "pytest>=9.0.3",
    "quak>=0.3.5",
]
